export PATH="$PATH:/path/to/release"

// copy this to your .bashrc or .zshrc
function {{alias}}() {
if [ $# -ge 2 ]
    first_argument="$1"
    # shift # Remove the first argument from the list

    if [ "$first_argument" = "move" ]; then
        # Execute the move action with the remaining arguments
        pindah "$@"
        cd "$2"
        # Replace the 'echo' line with your actual move logic
    elif [ "$first_argument" = "save" ]; then
        pindah "$@"
    elif [ "$first_argument" = "--help" ]; then
        pindah --help
    else
        # Invalid first argument
        echo "Invalid first argument: $first_argument"
    fi
}